package threesbrain.neuralnetwork

object GeneticAlgorithm {
    def train(numGenerations: Int, populationSize: Int, scoreFun: (NeuralNetwork) => Double,
              numInputs: Int, numOutputs: Int, numLayers: Int): NeuralNetwork = {
        ???
    }
}